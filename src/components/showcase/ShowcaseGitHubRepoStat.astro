---
interface Props {
  count: number;
  name: string;
}

const { count, name } = Astro.props;
---

<div class="flex flex-row gap-3">
  <slot />
  <div class="flex flex-col">
    <span class="text-xl">{count}</span>
    <span class="text-sm">{name}</span>
  </div>
</div>
