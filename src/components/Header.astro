---
import type { Props } from '@astrojs/starlight/props';
import Default from '@astrojs/starlight/components/Header.astro';
import ProgressScroll from './ProgressScroll.astro';

const displayProgressScroll =
	Astro.props.entry.slug.startsWith('guide') ||
	(Astro.props.entry.slug.startsWith('articles/') &&
		!Astro.props.entry.slug.startsWith('articles/tags') &&
		!Astro.props.entry.slug.startsWith('articles/authors') &&
		!Astro.props.entry.slug.match('articles/[0-9]*$'));
---

<Default {...Astro.props}>
	<slot />
</Default>

{displayProgressScroll && <ProgressScroll />}
